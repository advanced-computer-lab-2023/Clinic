import { Box } from '@mui/material'

export function Patient() {
  // const {id} = useParams()
  // const query = useQuery({
  //     queryKey: ['get-patient'],
  //     queryFn: () => getPatient(id),
  // })

  return (
    <Box sx={{ height: 400, width: '100%' }}>
      {
        //display patient details in query.data
      }
    </Box>
  )
}
