name: CI

on: [push]

jobs:
  format-and-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install packages
        run: npm install

      - name: Run tests
        run: cd server; npm test
